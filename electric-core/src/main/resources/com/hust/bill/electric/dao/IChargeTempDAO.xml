<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hust.bill.electric.dao.IChargeTempDAO">

	<insert id="insert">
    <if test="array.length == 0">
      select count(*) from e_record_charge_temp
    </if>
		<if test="array.length != 0">
			insert into e_record_charge_temp(buildingName, roomName, dateTime, power, money) values
			<foreach collection="array" item="item" index="index" separator=",">
				(#{item.buildingName, jdbcType=VARCHAR}, #{item.roomName, jdbcType=VARCHAR}, 
				 #{item.dateTime, jdbcType=DATE}, #{item.chargePower, jdbcType=FLOAT}, #{item.chargeMoneyW, jdbcType=FLOAT})
			</foreach>
		</if>
	</insert>

	<select id="getAll">
		select buildingName, roomName, dateTime, power, money from e_record_charge_temp
	</select>
	
	<delete id="truncate">
    truncate table e_record_charge_temp
  </delete>


	
</mapper>